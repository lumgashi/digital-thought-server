import { ICustomResponse } from '../interfaces';

/**
 * Get custom response as object
 * @param {Object} response - get an object as customResponse interface
 * @returns {Object} - return an object of all keys in customResponse interfce
 */
export function customResponse(response: ICustomResponse): ICustomResponse {
  return response;
}
